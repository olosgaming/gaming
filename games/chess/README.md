# Python Chess Engine

## Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

## Running
Run the engine:
```bash
python main.py
```

## Structure
- `main.py`: Entry point.
- `engine.py`: Core logic (to be created).
